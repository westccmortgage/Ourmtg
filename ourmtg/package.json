{
  "name": "ourmtg",
  "version": "0.1.0",
  "private": true,
  "description": "OurMTG — borrower/Realtor/processor operating layer on top of GRCRM (source of truth) + WCCI.online (AI strategy).",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "for f in netlify/functions/*.mjs netlify/functions/_lib/*.mjs; do node --check \"$f\"; done && echo ok",
    "test": "node --test tests/*.test.mjs src/domain/*.test.js",
    "test:domain": "node --test src/domain/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "nodemailer": "^6.9.0",
    "qrcode": "^1.5.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
